language: cpp
compiler:
  - gcc

before_install:
  - sudo sh -c "echo 'deb http://download.opensuse.org/repositories/home:/martinsiggel/xUbuntu_13.10/ /' >> /etc/apt/sources.list.d/tigl.list"
  - sudo apt-get update
  - sudo apt-get install libtixi-dev liboce-dev
  
before_script:
  - mkdir build
  - cd build

script: 
  - cmake ..
  - make test


