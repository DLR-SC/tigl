// Copyright (c) 2020 RISC Software GmbH
//
// This file was generated by CPACSGen from CPACS XML Schema (c) German Aerospace Center (DLR/SC).
// Do not edit, all changes are lost when files are re-generated.
//
// Licensed under the Apache License, Version 2.0 (the "License")
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include <cassert>
#include "CCPACSSystemElements.h"
#include "CPACSSysElemFuelCell.h"
#include "CPACSSysElemFuelCells.h"
#include "CTiglError.h"
#include "CTiglLogging.h"
#include "CTiglUIDManager.h"
#include "CTiglUIDObject.h"
#include "TixiHelper.h"

namespace tigl
{
namespace generated
{
    CPACSSysElemFuelCells::CPACSSysElemFuelCells(CCPACSSystemElements* parent, CTiglUIDManager* uidMgr)
        : m_uidMgr(uidMgr)
    {
        //assert(parent != NULL);
        m_parent = parent;
    }

    CPACSSysElemFuelCells::~CPACSSysElemFuelCells()
    {
    }

    const CCPACSSystemElements* CPACSSysElemFuelCells::GetParent() const
    {
        return m_parent;
    }

    CCPACSSystemElements* CPACSSysElemFuelCells::GetParent()
    {
        return m_parent;
    }

    const CTiglUIDObject* CPACSSysElemFuelCells::GetNextUIDParent() const
    {
        if (m_parent) {
            return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDObject* CPACSSysElemFuelCells::GetNextUIDParent()
    {
        if (m_parent) {
            return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDManager& CPACSSysElemFuelCells::GetUIDManager()
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    const CTiglUIDManager& CPACSSysElemFuelCells::GetUIDManager() const
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    void CPACSSysElemFuelCells::ReadCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath)
    {
        // read element fuelCell
        if (tixi::TixiCheckElement(tixiHandle, xpath + "/fuelCell")) {
            tixi::TixiReadElements(tixiHandle, xpath + "/fuelCell", m_fuelCells, 1, tixi::xsdUnbounded, this, m_uidMgr);
        }

    }

    void CPACSSysElemFuelCells::WriteCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath) const
    {
        // write element fuelCell
        tixi::TixiSaveElements(tixiHandle, xpath + "/fuelCell", m_fuelCells);

    }

    const std::vector<std::unique_ptr<CPACSSysElemFuelCell>>& CPACSSysElemFuelCells::GetFuelCells() const
    {
        return m_fuelCells;
    }

    std::vector<std::unique_ptr<CPACSSysElemFuelCell>>& CPACSSysElemFuelCells::GetFuelCells()
    {
        return m_fuelCells;
    }

    size_t CPACSSysElemFuelCells::GetFuelCellCount() const
    {
        return m_fuelCells.size();
    }

    size_t CPACSSysElemFuelCells::GetFuelCellIndex(const std::string& UID) const
    {
        for (size_t i=0; i < GetFuelCellCount(); i++) {
            const std::string tmpUID(m_fuelCells[i]->GetUID());
            if (tmpUID == UID) {
                return i+1;
            }
        }
        throw CTiglError("Invalid UID in CPACSSysElemFuelCells::GetFuelCellIndex", TIGL_UID_ERROR);
    }

    CPACSSysElemFuelCell& CPACSSysElemFuelCells::GetFuelCell(size_t index)
    {
        if (index < 1 || index > GetFuelCellCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSysElemFuelCell>>::GetFuelCell", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_fuelCells[index];
    }

    const CPACSSysElemFuelCell& CPACSSysElemFuelCells::GetFuelCell(size_t index) const
    {
        if (index < 1 || index > GetFuelCellCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSysElemFuelCell>>::GetFuelCell", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_fuelCells[index];
    }

    CPACSSysElemFuelCell& CPACSSysElemFuelCells::GetFuelCell(const std::string& UID)
    {
        for (auto& elem : m_fuelCells ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSSysElemFuelCells::GetFuelCell. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }

    const CPACSSysElemFuelCell& CPACSSysElemFuelCells::GetFuelCell(const std::string& UID) const
    {
        for (auto& elem : m_fuelCells ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSSysElemFuelCells::GetFuelCell. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }


    CPACSSysElemFuelCell& CPACSSysElemFuelCells::AddFuelCell()
    {
        m_fuelCells.push_back(make_unique<CPACSSysElemFuelCell>(this, m_uidMgr));
        return *m_fuelCells.back();
    }

    void CPACSSysElemFuelCells::RemoveFuelCell(CPACSSysElemFuelCell& ref)
    {
        for (std::size_t i = 0; i < m_fuelCells.size(); i++) {
            if (m_fuelCells[i].get() == &ref) {
                m_fuelCells.erase(m_fuelCells.begin() + i);
                return;
            }
        }
        throw CTiglError("Element not found");
    }

} // namespace generated
} // namespace tigl
