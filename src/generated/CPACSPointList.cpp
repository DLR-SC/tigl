// Copyright (c) 2020 RISC Software GmbH
//
// This file was generated by CPACSGen from CPACS XML Schema (c) German Aerospace Center (DLR/SC).
// Do not edit, all changes are lost when files are re-generated.
//
// Licensed under the Apache License, Version 2.0 (the "License")
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include <cassert>
#include <CCPACSPoint.h>
#include "CPACSPointList.h"
#include "CPACSStructuralProfile3D.h"
#include "CTiglError.h"
#include "CTiglLogging.h"
#include "CTiglUIDManager.h"
#include "CTiglUIDObject.h"
#include "TixiHelper.h"

namespace tigl
{
namespace generated
{
    CPACSPointList::CPACSPointList(CPACSStructuralProfile3D* parent, CTiglUIDManager* uidMgr)
        : m_uidMgr(uidMgr)
    {
        //assert(parent != NULL);
        m_parent = parent;
    }

    CPACSPointList::~CPACSPointList()
    {
    }

    const CPACSStructuralProfile3D* CPACSPointList::GetParent() const
    {
        return m_parent;
    }

    CPACSStructuralProfile3D* CPACSPointList::GetParent()
    {
        return m_parent;
    }

    const CTiglUIDObject* CPACSPointList::GetNextUIDParent() const
    {
        return m_parent;
    }

    CTiglUIDObject* CPACSPointList::GetNextUIDParent()
    {
        return m_parent;
    }

    CTiglUIDManager& CPACSPointList::GetUIDManager()
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    const CTiglUIDManager& CPACSPointList::GetUIDManager() const
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    void CPACSPointList::ReadCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath)
    {
        // read element point
        if (tixi::TixiCheckElement(tixiHandle, xpath + "/point")) {
            tixi::TixiReadElements(tixiHandle, xpath + "/point", m_points, 2, tixi::xsdUnbounded, this, m_uidMgr);
        }

    }

    void CPACSPointList::WriteCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath) const
    {
        // write element point
        tixi::TixiSaveElements(tixiHandle, xpath + "/point", m_points);

    }

    const std::vector<std::unique_ptr<CCPACSPoint>>& CPACSPointList::GetPoints() const
    {
        return m_points;
    }

    std::vector<std::unique_ptr<CCPACSPoint>>& CPACSPointList::GetPoints()
    {
        return m_points;
    }

    size_t CPACSPointList::GetPointCount() const
    {
        return m_points.size();
    }

    size_t CPACSPointList::GetPointIndex(const std::string& UID) const
    {
        for (size_t i=0; i < GetPointCount(); i++) {
            const boost::optional<std::string> tmpUID(*m_points[i]->GetUID());
            if (tmpUID == UID) {
                return i+1;
            }
        }
        throw CTiglError("Invalid UID in CPACSPointList::GetPointIndex", TIGL_UID_ERROR);
    }

    CCPACSPoint& CPACSPointList::GetPoint(size_t index)
    {
        if (index < 1 || index > GetPointCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CCPACSPoint>>::GetPoint", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_points[index];
    }

    const CCPACSPoint& CPACSPointList::GetPoint(size_t index) const
    {
        if (index < 1 || index > GetPointCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CCPACSPoint>>::GetPoint", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_points[index];
    }

    CCPACSPoint& CPACSPointList::GetPoint(const std::string& UID)
    {
        for (auto& elem : m_points ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSPointList::GetPoint. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }

    const CCPACSPoint& CPACSPointList::GetPoint(const std::string& UID) const
    {
        for (auto& elem : m_points ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSPointList::GetPoint. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }


    CCPACSPoint& CPACSPointList::AddPoint()
    {
        m_points.push_back(make_unique<CCPACSPoint>(this, m_uidMgr));
        return *m_points.back();
    }

    void CPACSPointList::RemovePoint(CCPACSPoint& ref)
    {
        for (std::size_t i = 0; i < m_points.size(); i++) {
            if (m_points[i].get() == &ref) {
                m_points.erase(m_points.begin() + i);
                return;
            }
        }
        throw CTiglError("Element not found");
    }

} // namespace generated
} // namespace tigl
