// Copyright (c) 2020 RISC Software GmbH
//
// This file was generated by CPACSGen from CPACS XML Schema (c) German Aerospace Center (DLR/SC).
// Do not edit, all changes are lost when files are re-generated.
//
// Licensed under the Apache License, Version 2.0 (the "License")
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include <cassert>
#include "CPACSDeckElements.h"
#include "CPACSSeatElement.h"
#include "CPACSSeatElements.h"
#include "CTiglError.h"
#include "CTiglLogging.h"
#include "CTiglUIDManager.h"
#include "CTiglUIDObject.h"
#include "TixiHelper.h"

namespace tigl
{
namespace generated
{
    CPACSSeatElements::CPACSSeatElements(CPACSDeckElements* parent, CTiglUIDManager* uidMgr)
        : m_uidMgr(uidMgr)
    {
        //assert(parent != NULL);
        m_parent = parent;
    }

    CPACSSeatElements::~CPACSSeatElements()
    {
    }

    const CPACSDeckElements* CPACSSeatElements::GetParent() const
    {
        return m_parent;
    }

    CPACSDeckElements* CPACSSeatElements::GetParent()
    {
        return m_parent;
    }

    const CTiglUIDObject* CPACSSeatElements::GetNextUIDParent() const
    {
        if (m_parent) {
            return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDObject* CPACSSeatElements::GetNextUIDParent()
    {
        if (m_parent) {
            return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDManager& CPACSSeatElements::GetUIDManager()
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    const CTiglUIDManager& CPACSSeatElements::GetUIDManager() const
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    void CPACSSeatElements::ReadCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath)
    {
        // read element seatElement
        if (tixi::TixiCheckElement(tixiHandle, xpath + "/seatElement")) {
            tixi::TixiReadElements(tixiHandle, xpath + "/seatElement", m_seatElements, 1, tixi::xsdUnbounded, this, m_uidMgr);
        }

    }

    void CPACSSeatElements::WriteCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath) const
    {
        // write element seatElement
        tixi::TixiSaveElements(tixiHandle, xpath + "/seatElement", m_seatElements);

    }

    const std::vector<std::unique_ptr<CPACSSeatElement>>& CPACSSeatElements::GetSeatElements() const
    {
        return m_seatElements;
    }

    std::vector<std::unique_ptr<CPACSSeatElement>>& CPACSSeatElements::GetSeatElements()
    {
        return m_seatElements;
    }

    size_t CPACSSeatElements::GetSeatElementCount() const
    {
        return m_seatElements.size();
    }

    size_t CPACSSeatElements::GetSeatElementIndex(const std::string& UID) const
    {
        for (size_t i=0; i < GetSeatElementCount(); i++) {
            const std::string tmpUID(m_seatElements[i]->GetUID());
            if (tmpUID == UID) {
                return i+1;
            }
        }
    }

    CPACSSeatElement& CPACSSeatElements::GetSeatElement(size_t index)
    {
        if (index < 1 || index > GetSeatElementCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSeatElement>>::GetSeatElement", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_seatElements[index];
    }

    const CPACSSeatElement& CPACSSeatElements::GetSeatElement(size_t index) const
    {
        if (index < 1 || index > GetSeatElementCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSeatElement>>::GetSeatElement", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_seatElements[index];
    }

    CPACSSeatElement& CPACSSeatElements::GetSeatElement(const std::string& UID)
    {
        for (auto& elem : m_seatElements ) {
            if (elem->GetUID() == UID)
                return *elem;
            throw CTiglError("Invalid UID in CPACSSeatElements::GetSeatElement. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
        }
    }

    const CPACSSeatElement& CPACSSeatElements::GetSeatElement(const std::string& UID) const
    {
        for (auto& elem : m_seatElements ) {
            if (elem->GetUID() == UID)
                return *elem;
            throw CTiglError("Invalid UID in CPACSSeatElements::GetSeatElement. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
        }
    }


    CPACSSeatElement& CPACSSeatElements::AddSeatElement()
    {
        m_seatElements.push_back(make_unique<CPACSSeatElement>(this, m_uidMgr));
        return *m_seatElements.back();
    }

    void CPACSSeatElements::RemoveSeatElement(CPACSSeatElement& ref)
    {
        for (std::size_t i = 0; i < m_seatElements.size(); i++) {
            if (m_seatElements[i].get() == &ref) {
                m_seatElements.erase(m_seatElements.begin() + i);
                return;
            }
        }
        throw CTiglError("Element not found");
    }

} // namespace generated
} // namespace tigl
