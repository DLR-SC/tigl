// Copyright (c) 2020 RISC Software GmbH
//
// This file was generated by CPACSGen from CPACS XML Schema (c) German Aerospace Center (DLR/SC).
// Do not edit, all changes are lost when files are re-generated.
//
// Licensed under the Apache License, Version 2.0 (the "License")
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include <cassert>
#include "CPACSSystemArchitecture.h"
#include "CPACSSystemConnection.h"
#include "CPACSSystemConnections.h"
#include "CTiglError.h"
#include "CTiglLogging.h"
#include "CTiglUIDManager.h"
#include "CTiglUIDObject.h"
#include "TixiHelper.h"

namespace tigl
{
namespace generated
{
    CPACSSystemConnections::CPACSSystemConnections(CPACSSystemArchitecture* parent, CTiglUIDManager* uidMgr)
        : m_uidMgr(uidMgr)
    {
        //assert(parent != NULL);
        m_parent = parent;
    }

    CPACSSystemConnections::~CPACSSystemConnections()
    {
    }

    const CPACSSystemArchitecture* CPACSSystemConnections::GetParent() const
    {
        return m_parent;
    }

    CPACSSystemArchitecture* CPACSSystemConnections::GetParent()
    {
        return m_parent;
    }

    const CTiglUIDObject* CPACSSystemConnections::GetNextUIDParent() const
    {
        if (m_parent) {
            if (m_parent->GetUID())
                return m_parent;
            else
                return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDObject* CPACSSystemConnections::GetNextUIDParent()
    {
        if (m_parent) {
            if (m_parent->GetUID())
                return m_parent;
            else
                return m_parent->GetNextUIDParent();
        }
        return nullptr;
    }

    CTiglUIDManager& CPACSSystemConnections::GetUIDManager()
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    const CTiglUIDManager& CPACSSystemConnections::GetUIDManager() const
    {
        if (!m_uidMgr) {
            throw CTiglError("UIDManager is null");
        }
        return *m_uidMgr;
    }

    void CPACSSystemConnections::ReadCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath)
    {
        // read element connection
        if (tixi::TixiCheckElement(tixiHandle, xpath + "/connection")) {
            tixi::TixiReadElements(tixiHandle, xpath + "/connection", m_connections, 1, tixi::xsdUnbounded, this, m_uidMgr);
        }

    }

    void CPACSSystemConnections::WriteCPACS(const TixiDocumentHandle& tixiHandle, const std::string& xpath) const
    {
        // write element connection
        tixi::TixiSaveElements(tixiHandle, xpath + "/connection", m_connections);

    }

    const std::vector<std::unique_ptr<CPACSSystemConnection>>& CPACSSystemConnections::GetConnections() const
    {
        return m_connections;
    }

    std::vector<std::unique_ptr<CPACSSystemConnection>>& CPACSSystemConnections::GetConnections()
    {
        return m_connections;
    }

    size_t CPACSSystemConnections::GetConnectionCount() const
    {
        return m_connections.size();
    }

    size_t CPACSSystemConnections::GetConnectionIndex(const std::string& UID) const
    {
        for (size_t i=0; i < GetConnectionCount(); i++) {
            const std::string tmpUID(m_connections[i]->GetUID());
            if (tmpUID == UID) {
                return i+1;
            }
        }
        throw CTiglError("Invalid UID in CPACSSystemConnections::GetConnectionIndex", TIGL_UID_ERROR);
    }

    CPACSSystemConnection& CPACSSystemConnections::GetConnection(size_t index)
    {
        if (index < 1 || index > GetConnectionCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSystemConnection>>::GetConnection", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_connections[index];
    }

    const CPACSSystemConnection& CPACSSystemConnections::GetConnection(size_t index) const
    {
        if (index < 1 || index > GetConnectionCount()) {
            throw CTiglError("Invalid index in std::vector<std::unique_ptr<CPACSSystemConnection>>::GetConnection", TIGL_INDEX_ERROR);
        }
        index--;
        return *m_connections[index];
    }

    CPACSSystemConnection& CPACSSystemConnections::GetConnection(const std::string& UID)
    {
        for (auto& elem : m_connections ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSSystemConnections::GetConnection. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }

    const CPACSSystemConnection& CPACSSystemConnections::GetConnection(const std::string& UID) const
    {
        for (auto& elem : m_connections ) {
            if (elem->GetUID() == UID)
                return *elem;
            }
            throw CTiglError("Invalid UID in CPACSSystemConnections::GetConnection. \""+ UID + "\" not found in CPACS file!" , TIGL_UID_ERROR);
    }


    CPACSSystemConnection& CPACSSystemConnections::AddConnection()
    {
        m_connections.push_back(make_unique<CPACSSystemConnection>(this, m_uidMgr));
        return *m_connections.back();
    }

    void CPACSSystemConnections::RemoveConnection(CPACSSystemConnection& ref)
    {
        for (std::size_t i = 0; i < m_connections.size(); i++) {
            if (m_connections[i].get() == &ref) {
                m_connections.erase(m_connections.begin() + i);
                return;
            }
        }
        throw CTiglError("Element not found");
    }

} // namespace generated
} // namespace tigl
